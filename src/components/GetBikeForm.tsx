import React from "react";
import { Box, Typography } from "@mui/material";

export default function GetBikeForm() {
  return (
    <Box sx={{ width: '100%' }}>
      <Typography variant="h5" color="#fff" fontWeight={700} mb={2}>
        Get a Bike
      </Typography>
      {/* TODO: Add form fields here */}
      <Typography color="#bbb">Form coming soon.</Typography>
    </Box>
  );
}
