{
  "name": "sureboda-mpesa-api",
  "version": "1.0.0",
  "description": "M-Pesa API proxy for SUREBODA app",
  "main": "api/mpesa-stk-push.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.x"
  }
}
